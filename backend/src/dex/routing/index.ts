/**
 * DEX API Routing Module (Pure GraphQL)
 * 
 * All routes now use GraphQL-only handlers.
 * Database dependencies have been completely removed.
 */

// Export all GraphQL-only routes and utilities
export * from './graphql-routes';
